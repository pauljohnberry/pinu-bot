import type { CharacterDefinition } from "../character.js";
import { pinuCharacter } from "./pinu.js";

// Placeholder until kiba gets its own dog-first art direction.
export const kibaCharacter: CharacterDefinition = {
  ...pinuCharacter,
  name: "kiba",
};
